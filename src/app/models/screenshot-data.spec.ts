import { ScreenshotData } from './screenshot-data';

describe('ScreenshotData', () => {
  it('should create an instance', () => {
    expect(new ScreenshotData("data1", "data2")).toBeTruthy();
  });
});
